.. highlight:: shell

============
Installation
============

At the command line::

    $ easy_install tontineseed

Or, if you have virtualenvwrapper installed::

    $ mkvirtualenv tontineseed
    $ pip install tontineseed
